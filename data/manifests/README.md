# Dataset registration manifests

This directory may contain committed `dataset-registration-manifest-v1` records only when they expose safe hashes, counts, validation summaries and opaque governance references. Raw paths, archive member names, participant identifiers and source bytes are prohibited.

No real dataset is registered in logical PR13. A future manifest must be produced by the local-only registration command after the matching registry permission, licence, consent, version and schema gates pass.
